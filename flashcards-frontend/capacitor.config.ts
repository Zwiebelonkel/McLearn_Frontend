import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mclearn.app',
  appName: 'McLearn',
  webDir: 'dist/flashcards-frontend/browser',
};

export default config;
